# MVP-0 Acceptance Record — 2026-08-13

## Decision

**Revision 2 acceptance candidate: PASS, pending independent review.**

The human operator clarified that ASD intentionally exposes selected service
ports rather than TCP 443 and authorized an otherwise unused published port,
provided existing services remain untouched. Bounded, identity-checked probes
proved that 10001 and 10002 were both free on ASD and reachable from lzr;
10001 was selected and 10002 remained unused. The direct outside-in endpoint
is now `https://122-51-70-33.sslip.io:10001`.

A fresh production Client on lzr connected directly to that endpoint with no
`--dev`, no insecure TLS flag, and a private `SSL_CERT_FILE` containing only
the fresh test CA public certificate. Local Playwright then passed direct
public HTTPS login, Secure/HttpOnly/Strict cookie checks, Terminal WSS/PTY/
resize, and Fake Agent SSE with two approved lzr commands. All twelve
Baseline-0 items therefore remain passed, and the former revision-0 TCP 443
blocker is superseded by the user-authorized deployment topology. The endpoint
uses a dedicated two-day test CA and is an MVP test deployment, not a generally
trusted production website.

## Revision 2 Public-Port Rerun

Run ID: `20260813T072250Z`.

- Existing port 80 remained owned by nginx. Verified listeners on 8080 and
  10000 timed out from lzr; verified listeners on 10001 and 10002 returned
  exact sentinels from lzr. Every temporary probe was checked by PID, process
  start time, executable and uid before TERM/join, and all probe listeners were
  removed. Only 10001 was subsequently used.
- A fresh 0700 run directory and fresh SQLite/secrets/test CA were created.
  The Server bootstrapped once, was joined, and restarted against the same new
  database with a seven-key runtime env containing no
  `AISUMMONER_ADMIN_PASSWORD`.
- The reviewed Server and Client hashes remained
  `d9e4bf8f25484af5c4fa0c9e1a6884af8fda2a19464817537c74b3291b6bd06d`
  and
  `03bab20d11dfa47d4341cd5ef8da9b9d11bd76ea6daf4938c08859d06ca2d747`.
- Caddy image
  `sha256:aac61abc4024c323602ccb9ee38bd68b147601f518626b2a055e06944e01c510`
  ran as `1001:1001`, host network, read-only, `restart=no`. It mounted the
  leaf certificate chain and leaf key, not the CA private key, and listened
  only on public 10001.
- lzr strict-CA `curl` and the production Client both reached the direct
  public endpoint. The Client ran as uid 1003, had no development/root override
  flags, owned no listener, and used a new 0700 identity with 0600 key and
  metadata.
- Playwright 1.60.0 `reclaim after unpair` passed in 2.5 seconds, proving login,
  one-use pairing/reuse rejection, Online metadata and a real lzr Terminal.
  `scoped TLS browser terminal and Agent smoke` passed in 3.5 seconds, proving
  HTTPS cookie flags, WSS Terminal/PTY/resize and SSE Agent hostname/uname
  approvals on lzr.
- From lzr, 8088, 10002, 14096 and 14097 were unreachable while 10001 passed
  strict TLS health. ASD showed Server only on `127.0.0.1:8088`, no OpenCode or
  Bridge listener in Fake mode, and no 10002 listener. nginx remained active,
  all six nginx hashes were unchanged, and the same four unrelated containers
  remained running.
- The revision-2 Server/Client logs totaled 5,914 bytes. Eleven non-empty
  categories—administrator password, Session/Pairing secrets, CA private-key
  marker, Terminal sentinels/input, and Agent commands—were all absent. The
  lzr pairing output was uid 1003, mode 0600, and truncated to zero immediately
  after private transfer.
- Freeze snapshot: Server PID `1594510`, uid 1001, RSS 19,460 KiB; Client PID
  `595619`, uid 1003, RSS 11,756 KiB; Caddy container
  `57646701ad0838148454451c86be533b461e5fd8446b6b66d19d50466a212008`.
  ASD had about 5.42 GiB available memory and lzr about 1.88 GiB, both above
  their gates.

Retained intermediate failures: the first new-run directory creation was
correctly denied by the root-owned parent and no file was created; root then
created only the two exact 0700 `myself` run roots. The first Caddy attempt
failed before binding because its generated global-options block was on one
line; the exited `restart=no` container was removed and the multiline config
passed. The first bootstrap sequence discarded its browser-only password copy
before login; because the new database was still unused, the exact Server was
joined, only that new database was deleted, and bootstrap was repeated with a
private 0600 browser credential copy. The first anchored Playwright grep found
zero tests and executed no test body; the list gate found all four tests and
the narrow unanchored invocations passed. No old run, nginx file or unrelated
container was modified by these corrections.

## Revision 0 Historical Topology And Versions

The remainder of this record preserves the independently reviewed revision-0
functional/lifecycle/OpenCode evidence. Statements below about TCP 443 and run
ID `20260813T024159Z` describe that historical snapshot and are superseded only
for the revision-2 public-listener decision above.

Run ID: `20260813T024159Z`.

| Host | Role | Runtime boundary |
| --- | --- | --- |
| local x86_64 | Playwright 1.60.0, Browser controller | Scoped forward `https://localhost:18443`; Node 26.5.1, npm 12.0.2 |
| ASD x86_64 | Server, SQLite, Caddy, OpenCode 1.18.11 | Server `127.0.0.1:8088`; OpenCode `127.0.0.1:14096`; Bridge `127.0.0.1:14097`; Caddy `*:443` |
| lzr x86_64 | production Remote Client only | Outbound WSS through scoped TLS path; no Client listening socket |

ASD used Go 1.24.4 and Docker 26.1.5. Server and OpenCode ran as uid/gid
`1001:1001`; the Client ran as `1003:1003`. The pinned OpenCode image ID was
`sha256:9d13dd1062fac8e921fd20910104de4ba10abe92612191e449b8cc14e612a174`;
the Caddy image ID was
`sha256:aac61abc4024c323602ccb9ee38bd68b147601f518626b2a055e06944e01c510`.

Deployed hashes:

- Server: `d9e4bf8f25484af5c4fa0c9e1a6884af8fda2a19464817537c74b3291b6bd06d`.
- Client (local transfer and lzr):
  `03bab20d11dfa47d4341cd5ef8da9b9d11bd76ea6daf4938c08859d06ca2d747`.
- canonical `opencode.json`:
  `61f93e1becfe74a108f5288fb687ff9e44a3d1e00b30e1900f56777e3f38253e`.
- canonical `remote_exec.ts`:
  `2700d7dc630a62398f0cadcc4785bdc0b32fd3561bdd82e652906145b939f75a`.
- pinned E2E lock:
  `e40bcca790113fe8af12aed2b5e331e880e2c28392e832b0c43f9d3c080d5feb`.

A later local state copy of the Server had hash `c7f5041d…`, but it was not
deployed or executed. `/proc/1530187/exe` and the ASD deployment path both
matched the recorded `d9e4bf8f…` runtime artifact.

## Build And Test Evidence

- Fresh isolated ASD source, with the tracked placeholder rather than a prior
  generated `dist`: `GOMAXPROCS=2 go test -count=1 -p 2 ./...` passed all 25
  targets (24 packages plus migrations with no tests). `go vet ./...` passed.
- The Origin hardening added during acceptance passed focused tests
  (`httpapi` 0.308 s, `agentapi` 0.570 s), 20 repeats (2.188/4.295 s), and
  race tests (38.493/9.270 s). A raw allowed-first duplicate `Origin` request
  changed from an incorrect authenticated-path 401 to 403 `ORIGIN_FORBIDDEN`
  for both Browser and Agent APIs. Terminal already had strict duplicate-Origin
  coverage.
- The first full Go staging run used generated Web assets and correctly failed
  only the clean-placeholder oracles in `staticweb`/Server. No product package
  failed. Recreating the test staging with the tracked placeholder made the
  exact full command pass; production assets were then restored before build.
- Final Web verification passed Vitest 8 files/22 tests and the production Vite
  build. The generated `dist` had two hashed assets and no placeholder page.
- The E2E package used exact Playwright `1.60.0`, one worker, zero retries, line
  reporting, and no screenshots, video or traces. Final `tsc --noEmit` and
  the one-test list gate passed.
- Fake full-chain browser phases passed: primary `fake-lifecycle` covered
  pairing through unpair/newest-wins/restart; the independent `reclaim` phase
  claimed the newly issued code and re-proved the Remote Terminal. The scoped
  TLS browser smoke passed one test in 3.9 s. The real OpenCode smoke passed one
  test after the bounded pending-tool timeout was raised from 30 to 120 seconds.

The first real OpenCode browser invocation used an anchored grep that matched
zero tests; no test body ran. The next run reached a real provider Session but
the model produced its pending tool just after the original 30-second browser
oracle. Database evidence showed an external Session and one unapproved,
unexecuted `remote_exec`. After the timeout-only harness correction, a fresh
Session passed. The abandoned Session finalized as failed with no approval;
the database had zero pending tools at freeze.

## Baseline-0 Twelve-Item Matrix

| # | Baseline item | Result | Evidence class and result |
| ---: | --- | --- | --- |
| 1 | Fresh Remote Client shows a pairing code | PASS | Live lzr Client with a new identity produced one expiring code into a uid-1003, mode-0600 file. The value was never written to this record. |
| 2 | Browser login and pairing | PASS | Live Playwright Browser → Server → lzr claim succeeded and the Device detail was rendered. HTTPS login also re-passed with Secure/HttpOnly/Strict cookie booleans. |
| 3 | Same pairing code cannot be reused | PASS | Live Browser second claim returned 400 `PAIRING_CODE_INVALID` with a request ID. |
| 4 | Device list shows Online | PASS | Live browser metadata and Online assertions passed for the lzr identity. |
| 5 | Terminal runs `uname -a` and an interactive PTY program | PASS | Real browser WSS → Terminal → Tunnel → embedded SSH on lzr returned lzr hostname, `uname`, uid, stdout/stderr/exit-7 sentinels, and a READY-gated interactive `read`. ASD/local hostname exclusions were asserted. |
| 6 | Browser resize matches remote `stty size` | PASS | Browser captured the actual `terminal.resize` frame and compared rows/columns with live lzr `stty size`. |
| 7 | Agent requests and executes `remote_exec` | PASS | Fake main flow executed hostname and uname through real lzr SSH with approvals. Separately, real OpenCode created exactly one `remote_exec` containing hostname+uname, approved once, exit 0, output non-empty. |
| 8 | WebUI displays command, authorization, exit and final answer | PASS | Browser assertions covered pending→started→output→completed, approve-once, approve-session, deny, exit 0, `truncated=false`, and assistant-only final text containing lzr hostname plus Linux while excluding ASD/local. |
| 9 | Network loss becomes Offline within 15 seconds | PASS | Deterministic stop-client barrier held the exact Client identity; the bounded 15-second browser oracle observed Offline, closed the existing Terminal, and verified a pending Agent tool failed `DEVICE_OFFLINE` without output. |
| 10 | Network recovery reconnects to Online | PASS | Same identity/Device ID reconnected without pairing. A replacement Client authenticated while the primary was SIGSTOPed within the 15-second heartbeat window, opened a real Terminal, then the exact old process was TERM→CONT joined. |
| 11 | Non-owner paths are rejected | PASS | The deployment intentionally has one administrator. Approved ownership-joined `httpapi`, `agentapi`, Terminal and Store integration tests provide cross-owner evidence; live unauthenticated and unknown Device/Session/Tool requests returned standard 401/404 boundaries. |
| 12 | One E2E smoke spans pairing to remote execution | PASS | Lock-versioned `fake-lifecycle` plus its fresh-code `reclaim` phase automated Browser login, claim/reuse, Terminal/PTY/resize, Agent approval/tool results, Offline/reconnect/newest-wins, restart and unpair against the real lzr Remote. |

## Security And Lifecycle Evidence

- Root refusal: the verified Client run as root without both development flags
  exited nonzero before creating an identity or connecting.
- Pairing expiry: an independent fresh code was held beyond the fixed ten-minute
  lifetime; claim returned 410 `PAIRING_CODE_EXPIRED` with a request ID after
  367 seconds of the recorded acceptance wait. All four scoped pairing-output
  files were uid 1003, mode 0600 and size 0 after claim/cleanup.
- Private identity: lzr state and identity directories were 0700; metadata and
  Ed25519 private key were 0600. The replacement Client RSS was 12,040 KiB,
  below 150 MiB, and it owned zero listening sockets.
- Server restart: the Server restarted against the same SQLite database without
  `AISUMMONER_ADMIN_PASSWORD`; login, Device and Agent history remained usable.
- Origin: missing/wrong/duplicate Origin fails before authentication on all
  mutation APIs; the acceptance-discovered allowed-first duplicate weakness in
  Browser/Agent APIs was fixed and revalidated as described above.
- Trusted proxy: Server trusted only exact immediate peer `127.0.0.1`; Caddy
  overwrote the dedicated source header. Two controlled source addresses kept
  Login limiter windows independent and two more kept Tunnel windows
  independent. Direct untrusted forged dedicated/XFF/Forwarded headers could
  not change the peer key. Missing/malformed headers from the exact trusted
  peer repeatedly returned 400 before Login/Tunnel limiter state.
- TLS: production Client ran without `--dev` and validated the scoped CA via a
  private `SSL_CERT_FILE`; Python strict TLS and Browser HTTPS/WSS/SSE also
  passed. No insecure-skip-verify Client path was used. Public certificate
  issuance/reachability remains blocked as stated in the Decision.
- Exposure: ASD 8088, 14096 and 14097 listened only on `127.0.0.1`; only Caddy
  listened on 443. lzr Client had no listener. Caddy and OpenCode used
  `restart=no`. Six nginx configuration hashes were byte-identical before and
  after, and the four unrelated ASD containers were preserved.
- Log redaction: 393,749 bytes from scoped Server, Client, OpenCode and Caddy
  logs were scanned against 19 non-empty categories: admin/browser password,
  Session/Pairing/Bridge secrets, OpenCode Basic Auth, three pairing codes,
  three private keys, an authenticated cookie, Terminal input/output sentinels,
  Fake/OpenCode prompts and the OpenCode command. Every category was
  `present=false`; the temporary scanner directory was deleted.
- Output truncation, execution timeout, no-executor-on-offline, cross-owner
  denial, HMAC/Bridge redaction and joined cleanup retain approved in-process
  race/regression evidence from Tasks 005–009 and the clean full test run.

## Real OpenCode Classification

`available`.

The pinned ASD-local OpenCode 1.18.11 sidecar was healthy with Basic Auth and
the selected free model. Root and embedded deny-by-default policy/tool hashes
matched. Server and sidecar shared the same absolute 0700 workspace while the
Bridge remained loopback-only. The successful external Session had one
`remote_exec`, command metadata contained both hostname and uname, decision
was `approve_once`, exit was 0, output was non-empty, Session state returned to
idle, and the assistant-only final message identified lzr/Linux while excluding
ASD/local. No Fake output was used for this classification.

## Resources And Running State At Freeze

- Local stayed above the 8-GiB/4-GiB gates during the final Node/Browser work.
- ASD stayed above 3 GiB available during builds and acceptance. At freeze the
  Server RSS was 79,104 KiB, OpenCode used about 636 MiB and Caddy about 18 MiB.
- lzr remained above 1.2 GiB available and ran no Node, Docker or build task.
- Running scoped components at review handoff: Server PID 1530187, Client PID
  524331, named Caddy and OpenCode containers, and two private SSH control
  sockets. Exact process UID/start-time/executable/hash checks preceded signals
  and lifecycle changes.

## Revision 0 Blocker History And Follow-Ups

### Superseded revision-0 blocker

- Public 443 ingress to ASD was blocked outside the host. Both local and lzr
  timed out; TLS-ALPN issuance reported a connection timeout even though Caddy
  was listening and host UFW allowed 443. Revision 2 supersedes this condition
  by explicit human authorization of already-published, otherwise unused TCP
  10001 and the successful direct public rerun recorded above.

### Alpha follow-ups

- Runtime Agent revocation tombstones grow for the process lifetime.
- A post-commit cleanup timeout may leave an uncooperative cleanup worker
  finishing in the background, though logical authorization is already gone.
- Vite reports a roughly 557-KiB JavaScript chunk; functional budgets passed,
  but code splitting remains an optimization.
- Replace the test-CA/scoped tunnels with a publicly trusted certificate before
  any non-test use.

## Revision 0 Scoped Stop And Recovery

Only the following run-owned paths are in scope:

- ASD: `/home/myself/.local/{opt,state}/aisummoner-task010/20260813T024159Z`
- lzr: `/home/myself/.local/{opt,state}/aisummoner-task010/20260813T024159Z`
- local: `/home/myself/.local/state/aisummoner-task010/20260813T024159Z`

Cleanup must verify PID/start time, executable path, UID and container name
before acting. Order: exact lzr Client TERM/join; close the lzr and ASD SSH
control sockets with `ssh -S <socket> -O exit`; exact ASD Server TERM/join;
stop/remove only the two Task010-named containers. Never use broad `pkill`,
`compose down`, shared-network deletion or nginx mutation. Retain the private
SQLite/state directories for review/recovery unless a human explicitly asks
to delete them. Secret-bearing env/certificate private-key/browser files must
be removed after independent review; no such value belongs in this record.

## Revision 2 Running Review State And Cleanup

Only these new run-owned paths are in scope:

- ASD: `/home/myself/.local/{opt,state}/aisummoner-task010/20260813T072250Z`
- lzr: `/home/myself/.local/{opt,state}/aisummoner-task010/20260813T072250Z`
- local temporary controller: `/tmp/aisummoner-task010-r2-20260813T072250Z`

The demo was left running only through the independent revision-2 review. After
the `APPROVED` decision, the exact Client PID 595619 and Server PID 1594510
were revalidated, TERM-joined, and the exact Caddy container was stopped and
removed. Revision-2 runtime/browser credentials, CA/leaf private material,
local E2E env/CA, `web/e2e/node_modules`, and Playwright output were deleted.
SQLite, Device identity, verified binaries and non-secret logs remain only for
audit/recovery. See
[`mvp-0-2026-08-13-r2-cleanup.md`](mvp-0-2026-08-13-r2-cleanup.md).
No broad `pkill`, `compose down`, nginx mutation, shared-network deletion or
revision-0 audit-state removal was used.
